
name: Build and Deploy to GitHub Pages (official)

on:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      name: CI - runner smoke test

      on:
        push:
          branches: [ main ]

      jobs:
        smoke:
          runs-on: ubuntu-latest

          steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Smoke test
              run: |
                echo "Runner smoke test - OK"
